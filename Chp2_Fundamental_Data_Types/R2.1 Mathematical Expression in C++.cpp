#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    double s = 0;
    int s0 = 0;
    int v0 = 0;
    int t = 0;
    int g = 0;
    int second_expression = v0 * t;
    double third_expression = 0.5 * g * pow(t, 2);

    s = s0 + second_expression + third_expression;

    double G = 0;
    int a = 0;
    int p = 0;
    int m1 = 0;
    int m2 = 0;
    const double pi = 3.14;
    int numerator = pow(a, 3);
    int denominator = pow(p, 2) * (m1 + m2);
    int first_expression_2 = 4 * pow(pi, 2);
    double second_expression_2 = numerator / denominator;

    G = first_expression_2 * second_expression_2;

    int F = 0;
    int V = 0;
    double FV = F * V;
    int P = 0;
    int PV = P * V;
    int I = 0;
    int N = 0;
    int T = 0;
    int INT = I * N * T;
    int yrs = 0;
    double brackets = 1 + (INT / 100);
    double second_expression_3 = pow(brackets, yrs);

    FV = PV * second_expression_3;

    double c = 0;
    int b = 0;
    int lambda = 0;
    int first_root_expression_4 = a * a;
    int second_root_expression_4 = b * b;
    double third_root_expression_4 = 2 * a * b * cos(lambda);
    double root_expression = first_root_expression_4 + second_root_expression_4 - third_root_expression_4;

    c = sqrt(root_expression);

    return 0;
}
